# Backend Production Environment Variables
# ⚠️ IMPORTANT: Change all sensitive values before deploying!

# Server Configuration
PORT=4000
ENVIRONMENT=production
ALLOWED_ORIGINS=http://localhost:3000,http://kuafcv.uz,https://kuafcv.uz,https://www.kuafcv.uz,http://195.158.16.142

# Database (Use managed PostgreSQL service for production)
DATABASE_URL=postgres://username:password@db-host:5432/kuafcv?sslmode=require

# Redis (Use managed Redis service for production)
REDIS_URL=redis-host:6379
REDIS_PASSWORD=your-redis-password

# JWT Secret (MUST be changed!)
# Generate with: openssl rand -base64 32
JWT_SECRET=CHANGE_THIS_TO_RANDOM_64_CHARACTER_SECRET

# OpenAI API
# Store in environment variable or secrets manager
OPENAI_API_KEY=${OPENAI_API_KEY}

# File Upload
MAX_FILE_SIZE=10485760
UPLOAD_DIR=./uploads

# Security
RATE_LIMIT_REQUESTS=100
RATE_LIMIT_WINDOW=60

# Logging
LOG_LEVEL=info
LOG_FORMAT=json
